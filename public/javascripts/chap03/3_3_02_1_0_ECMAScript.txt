ECMAScript(ES)는 공식적인 자바스크립트 모듈혁식이다. Node에서는 아직CommonJS모듈을 많이 쓰지만
ES모듈이 표준으로 정해지면서 ES모듈을 사용하는 비율이 늘어나는 추세다.
웹브라우저에서도 ES모듈을 사용할수있고, Node에서도 같은 모듈 형식을 사용할수있다.
비전에 사용했던 모듈(3_3_01~~~~.js)를 ES 모듈 스타일로 변경해보기로 한다.
(오픈노트 표준도 ECMAScript기준으로 변경해보려고 한다.)
 